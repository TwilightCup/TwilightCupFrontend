# 拷贝为 .env 后按需修改。
#
# 两种运行模式：
#   1) 开发模式（npm run dev）：VITE_BACKEND_URL 留空，走 Vite 同源代理
#      （/api → REST、/ws → WebSocket），无需后端配 CORS。
#   2) 静态预览（npm run build + npm run preview）：【必须】设置 VITE_BACKEND_URL，
#      浏览器直连后端，需后端放行 CORS（后端已 allow_origins=["*"]）。
#      后端需监听 0.0.0.0 且防火墙放行端口，客户端才能访问到。
#      跨机访问加 --host：npm run preview -- --host

# 直连后端的绝对地址（协议+主机+端口），如 http://localhost:8000 或 http://192.168.1.10:8000
VITE_BACKEND_URL=

# 开发模式下也可不改 .env，直接在启动时指定 Vite 代理目标：
#   TWILIGHT_BACKEND=http://192.168.1.10:8000 npm run dev
