# 项目协作约定

- 每次完成修改并通过适用的检查后，主动创建 Git 提交，无需再次确认。
- 在 `dev` 分支上提交时，一律使用 `git -c commit.gpgsign=false commit --no-gpg-sign`，不使用 GPG 签名，也不要求解锁签名密钥。
- 未经用户明确授权，严禁向 `main` 分支提交或推送；自动提交约定不适用于 `main`。提交和推送前确认当前分支，推送时明确指定目标分支。
- 只提交当前任务涉及的修改，保留工作区中用户已有的其他修改。
